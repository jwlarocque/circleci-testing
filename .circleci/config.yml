version: 2.1

orbs:
  python: circleci/python@0.2.1

jobs:
  build:
    docker:
      - image: circleci/python:2.7
    steps:
      - run: echo $PWD
      - checkout
      - python/install-deps
      - run: ls -al
      - run: echo "wow all done"
      - run: python test.py
      - run: pytest test.py --verbose --junit-xml test-reports/results.xml

workflows:
  version: 2.1
  "Test":
    jobs:
      - build